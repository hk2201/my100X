const arr = [1, 2, 3, 4, 5, 6];

const newarr = arr.map((i) => {
  return i * 2;
});

const add = arr.map((i) => {
  return i + 2;
});

const subs = arr.map((i) => {
  return i - 2;
});

const mult = arr.map((i) => {
  return 1 * 2 * 3;
});

const modd = arr.map((i) => {
  return i % 2;
});

console.log(newarr);
